package com.example.provacomponenti.Prova.DataBaseProva
/*

import android.app.Application
import androidx.lifecycle.AndroidViewModel
import androidx.lifecycle.LiveData
import androidx.lifecycle.viewModelScope
import com.example.provacomponenti.Database.Motor
import com.example.provacomponenti.Database.Track
import kotlinx.coroutines.launch

class ViewModelProva (appObj: Application):AndroidViewModel(appObj){
    private val repo:RepositoryProva = RepositoryProva(appObj)


    //--------------------------Moto---------------
    fun getAllMotors():List<Motor>{
        return repo.readAllMoto
    }

    fun insertMotor(motor: Motor){
        viewModelScope.launch {
            repo.insertMoto(motor = motor)
        }
    }


    //--------------------------Track------------------

    fun getAllTracks():LiveData<List<Track>>{
        return repo.readAllTrack
    }
    fun insertTrack(track: Track){
        viewModelScope.launch {
            repo.insertTrack(track = track)
        }
    }
}*/
